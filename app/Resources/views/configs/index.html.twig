{% extends 'base.html.twig' %}

{% block body %}
    <h1>Configs list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Configname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for config in configs %}
            <tr>
                <td><a href="{{ path('admin_configs_show', { 'id': config.id }) }}">{{ config.id }}</a></td>
                <td>{{ config.configname }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_configs_show', { 'id': config.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_configs_edit', { 'id': config.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_configs_new') }}">Create a new config</a>
        </li>
    </ul>
{% endblock %}
