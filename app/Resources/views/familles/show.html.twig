{% extends 'base.html.twig' %}

{% block body %}
    <h1>Famille</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famille.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ famille.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_familles_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_familles_edit', { 'id': famille.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
